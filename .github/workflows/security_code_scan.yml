name: Security Code Scan

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  semgrep:
    name: Semgrep
    runs-on: ubuntu-latest

    container:
      image: semgrep/semgrep

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Semgrep
        id: semgrep
        continue-on-error: true
        run: |
          semgrep scan \
            --config p/default \
            --severity ERROR \
            --json \
            --sarif \
            --output semgrep.json

      - name: Generate SARIF
        if: always()
        run: |
          semgrep scan \
            --config p/default \
            --severity ERROR \
            --sarif \
            --output semgrep.sarif

      - name: Create PR Summary
        if: always() && github.event_name == 'pull_request'
        run: |
          python3 - <<'PY'
          import json

          with open("semgrep.json", "r", encoding="utf-8") as f:
              data = json.load(f)

          results = data.get("results", [])

          with open("comment.md", "w", encoding="utf-8") as out:
              out.write("## Semgrep Security Scan\n\n")

              if not results:
                  out.write(
                      "No **ERROR-level** security findings detected. :white_check_mark:\n"
                  )
              else:
                  total = len(results)

                  out.write(
                      f"**{total} ERROR-level finding(s) detected.**\n\n"
                  )

                  out.write("| Severity | Finding | File | Line |\n")
                  out.write("|---|---|---|---:|\n")

                  for result in results[:5]:
                      extra = result.get("extra", {})
                      metadata = extra.get("metadata", {})

                      severity = extra.get("severity", "ERROR")

                      finding = metadata.get(
                          "name",
                          extra.get(
                              "message",
                              "No description available"
                          )
                      )

                      file_path = result.get("path", "N/A")

                      line = result.get(
                          "start",
                          {}
                      ).get(
                          "line",
                          "N/A"
                      )

                      finding = (
                          str(finding)
                          .replace("|", "\\|")
                          .replace("\n", " ")
                      )

                      file_path = (
                          str(file_path)
                          .replace("|", "\\|")
                          .replace("\n", " ")
                      )

                      if len(finding) > 70:
                          finding = finding[:67] + "..."

                      if len(file_path) > 50:
                          file_path = "..." + file_path[-47:]

                      out.write(
                          f"| {severity} | {finding} | "
                          f"`{file_path}` | {line} |\n"
                      )

                  if total > 5:
                      out.write(
                          f"\n> Showing **5 of {total}** findings. "
                          "See GitHub Code Scanning for the complete results.\n"
                      )
          PY

      - name: Comment on PR
        if: always() && github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: semgrep-security
          recreate: true
          path: comment.md

      - name: Verify SARIF
        if: always()
        run: |
          echo "Checking SARIF file..."
          ls -lh semgrep.sarif

      - name: Upload Semgrep SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: ${{ github.workspace }}/semgrep.sarif
          category: semgrep

      - name: Block PR on ERROR findings
        if: steps.semgrep.outcome == 'failure'
        run: |
          echo "Semgrep found ERROR-level security findings."
          echo "Fix the security findings before merging this PR."
          exit 1
